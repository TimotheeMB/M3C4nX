public class Sin extends Function {

    //CONSTRUCTORS
    public Sin(Variable variable) {
        super(variable);
    }

    //TOSTRING
    public String toString() {
        return "sin(" + variable + ')';
    }
}
